# Hình của Chương 3 (kiến thức nền tảng)

| File | Nhãn LaTeX | Nội dung |
|---|---|---|
| `arch-phantang.png` | `fig:kientruc_phantang` | Mô hình phân tầng: trình bày, điều khiển, nghiệp vụ, truy cập dữ liệu, CSDL; ánh xạ vào CoSpace (xoay ngang) |
| `race-condition.png` | `fig:race_condition` | Tương tranh khi hai người cùng đặt một chỗ: không bảo vệ, chỉ ràng buộc `EXCLUDE`, và khóa tư vấn kết hợp `EXCLUDE` |
| `cospace-race.drawio` | | Nguồn của hình tương tranh, mở bằng draw.io để chỉnh |

`arch-phantang` nằm trong file nguồn `../Chuong6/cospace-architecture.drawio` cùng ba sơ đồ kiến trúc khác.
Cách sinh lại: xem `../../tools/uml/README.md`.
